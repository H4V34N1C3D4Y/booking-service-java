spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/booking_service
    username: booking_admin
    password: admin_booking

  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    enabled: true